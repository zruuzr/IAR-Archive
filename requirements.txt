# IAR Archive — Python dependencies
#
# Hardening baseline — September 2026
#
# Compatible-release pins (~=) keep each dependency within the
# same minor release series while allowing patch updates.
#
# Dependabot monitors these dependencies weekly.
#
# Python CI baseline: 3.13

# Runtime
google-genai~=2.25.0
firecrawl-anydoc~=0.2.4   # provides the `anydoc` module
pypdf~=6.19.0
groq~=1.7.0

# Testing
pytest~=8.3.0
pytest-cov~=6.0.0
pytest-mock~=3.14.0
