[1mdiff --git a/.github/workflows/auto_process.yml b/.github/workflows/auto_process.yml[m
[1mindex 7cc20ad..e50f498 100644[m
[1m--- a/.github/workflows/auto_process.yml[m
[1m+++ b/.github/workflows/auto_process.yml[m
[36m@@ -67,12 +67,6 @@[m [mjobs:[m
           git config --global user.name "github-actions[bot]"[m
           git config --global user.email "github-actions[bot]@users.noreply.github.com"[m
 [m
[31m-          if ! git pull --rebase origin "${BRANCH}"; then[m
[31m-            echo "::error title=Rebase Failed::git pull --rebase failed on branch ${BRANCH}"[m
[31m-            git rebase --abort || true[m
[31m-            exit 1[m
[31m-          fi[m
[31m-[m
           git add books.json[m
           if [ -d covers ]; then[m
             git add covers/[m
[36m@@ -85,6 +79,12 @@[m [mjobs:[m
 [m
           git commit -m "${COMMIT_MSG}"[m
 [m
[32m+[m[32m          if ! git pull --rebase origin "${BRANCH}"; then[m
[32m+[m[32m            echo "::error title=Rebase Failed::git pull --rebase failed on branch ${BRANCH}"[m
[32m+[m[32m            git rebase --abort || true[m
[32m+[m[32m            exit 1[m
[32m+[m[32m          fi[m
[32m+[m
           for i in 1 2 3; do[m
             if git push origin "${BRANCH}"; then[m
               echo "Push succeeded on attempt ${i}."[m
